{
  "name": "run-in-everything",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "postinstall": "yarn playwright install",
    "nodejs": "node node.mjs",
    "firefox": "playwright test --browser firefox",
    "chromium": "playwright test --browser chromium",
    "safari": "playwright test --browser webkit",
    "test": "yarn nodejs; yarn chromium; yarn firefox; yarn safari"
  },
  "dependencies": {
    "@playwright/test": "^1.17.1"
  }
}
